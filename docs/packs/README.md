# Future Toolbelt packs

Stub only — **not** implemented in v0.1.0.

Planned growth (flat `skills/<name>/` under the plugin root when ready):

| Pack | Intent |
|------|--------|
| `quality-*` | Code quality / standards skills |
| `workflow-*` | Parallel development / process workflows |

Keep new rules **intelligent / opt-in** by default. Avoid always-apply sprawl across every repo that installs Toolbelt.
