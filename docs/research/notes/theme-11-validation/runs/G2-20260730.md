---
title: "Theme 11 run log — G2 reproduce-bug"
status: draft
theme: theme-11-validation
surface_id: G2
created: 2026-07-30
---

# G2 — reproduce-bug (pocket smoke)

| Field | Value |
|-------|-------|
| Surface | `reproduce-bug` |
| Date | 2026-07-30 |
| Smoke artifact | `docs/research/notes/theme-11-validation/runs/artifacts/REPRO-smoke-add.md` |
| Verdict | **PASS** |

## Smoke prompt (executed)

> Using `reproduce-bug` on docs/research/fixtures/smoke-app (read-only app.py). Prove add() bug with failing test output. Write light dossier to runs/artifacts/REPRO-smoke-add.md. Do not fix app.py.

## Smoke result (one line)

Proved deterministic `add(2,3)==6` via failing `python test_app.py` (`AssertionError`); light dossier written; `app.py` left buggy.

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `reproduce-bug` | pass | Exact announce in-run: “Using `reproduce-bug`.” |
| C2 | Never patches product fix | pass | `app.py` still `return a + b + 1`; post-repro `add(2,3)` → `6`; only artifact under `runs/artifacts/`. |
| C3 | Light dossier 8 fields / status | pass | `REPRO-smoke-add.md` has Status `DETERMINISTIC` + fields 1–8 (symptom, command, failing output, triggers, ledger, attempts, handoff). |
| C4 | Repro must fail | pass | Failing output kept: `assert add(2, 3) == 5` → `AssertionError`. |

## Anti-patterns

| # | Must not | Observed? | Notes |
|---|----------|-----------|-------|
| A1 | Fix the bug in this skill | no | No product patch; handoff points to `systematic-debug`. |

## Evidence quotes (from this run)

1. Announce: `Using \`reproduce-bug\`.`
2. Product still buggy: `return a + b + 1  # BUG: off-by-one` in `docs/research/fixtures/smoke-app/app.py`; direct call printed `6`.
3. Dossier status: `` `DETERMINISTIC` `` in `REPRO-smoke-add.md` §1.
4. Failing output: `AssertionError` on `assert add(2, 3) == 5` from `python test_app.py`.

## Verdict rationale

All four claims pass; A1 not observed. Skill announced, proved a failing repro without fixing `app.py`, and wrote an 8-field light dossier with honest `DETERMINISTIC` status.
