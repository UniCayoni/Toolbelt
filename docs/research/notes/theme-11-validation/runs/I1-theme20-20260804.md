---
title: "Theme 11 run log — I1 guide-implementation (Theme 20 rename smoke)"
status: draft
theme: theme-11-validation
surface_id: I1
created: 2026-08-04
skill: guide-implementation
aligned_with:
  - docs/research/notes/theme-11-validation/claim-cards/i1-implementation-router.md
  - docs/research/reports/theme-14-pocket-routers.md
  - docs/research/reports/theme-20-guide-rename.md
---

# I1 — guide-implementation (Theme 20 rename smoke)

**Using `guide-implementation`**.

| Field | Value |
|-------|-------|
| Surface | `guide-implementation` (formerly `implementation-router`) |
| Date | 2026-08-04 |
| Lane | in-session pocket/controller |
| Scope | Classify / wire / checklist / handoff only — no plan file, no code, no `skills/` or `rules/` edits |
| Verdict | **PASS** |

## Part A — full feature wire

Ask: design for smoke-app `add()` fix accepted at `docs/design/smoke-add-design.md` (hypothetical). Wire Implementation pocket only; checklist + handoff; do not plan or code.

| Field | Value |
|-------|-------|
| Impl ask | **full-ladder** |
| Design/ADR | Hypothetical accepted design at `docs/design/smoke-add-design.md` — smoke premise only; not treated as live product law beyond the fixture |
| Trivial skip | no |

Structured handoff:

```text
Goal: Wire Implementation pocket for smoke-app add() fix after accepted design.
Prior actions / results: Design gate assumed accepted at docs/design/smoke-add-design.md (hypothetical smoke premise).
Key facts + where from: Fixture ask = add() fix (Theme 11 smoke-app lineage / claim card I1 Part A).
Open question for next leaf: Durable plan path + Done-when/Verify; Meta ready before execute.
Constraints: Routing / checklist / handoff only — do not write plan body; do not implement.
```

Wire plan:

```text
- [x] implementation-plan
- [x] implementation-plan-verify → Meta ready
- [x] implementation-execute (single-worker lean for small fix)
- [ ] implementation-execute-subagents — N/A (not multi-task / fresh-agents ask)
- [x] implementation-execute-verify (non-trivial / EOP)
- [ ] On verify-fail / unclear Critical → hand off guide-debug — N/A until a leaf exits fail
```

Compose note: leaves named only; **no** Plan/Execute spines pasted. Selection ≠ solving: **no** WBS/tasks authored. Leaves not invoked (smoke stop after wire).

Exit: ready for human / next leaf would be **Using `implementation-plan`**.

## Part B — verify-only

Ask: Meta ready plan exists; greens done; need EOP execute-verify only. Wire; do not implement.

| Field | Value |
|-------|-------|
| Impl ask | **execute-verify** |
| Design/ADR | n/a for this stage (plan Meta ready given) |
| Trivial skip | n/a |

Structured handoff:

```text
Goal: Run EOP implementation-execute-verify only on Meta-ready plan with greens done.
Prior actions / results: Plan exists; Meta ready; task greens done (smoke given).
Key facts + where from: Claim card I1 Part B / user ask.
Open question for next leaf: Verify pass/fail and any Critical gaps for human or happy-path next stage.
Constraints: Wire with skips; do not implement; do not re-run plan/execute.
```

Wire plan:

```text
- [ ] implementation-plan — N/A (plan exists)
- [ ] implementation-plan-verify — N/A (Meta ready given)
- [ ] implementation-execute — N/A (greens done)
- [ ] implementation-execute-subagents — N/A
- [x] implementation-execute-verify
- [ ] On verify-fail / unclear Critical → hand off guide-debug — only if verify-fail later
```

Not a full ladder. Leaf not invoked (smoke stop after wire).

Exit: ready for human / next leaf would be **Using `implementation-execute-verify`**.

## Part C — single leaf / skip

Ask: only run `implementation-plan-verify` on the existing plan. Wire or document leaf-direct.

| Field | Value |
|-------|-------|
| Impl ask | **plan+verify** lean → skill documents **skip** when user named a single clear leaf |
| Exit | Leaf-direct: **Using `implementation-plan-verify`** — router not mandatory |

Documented skip:

```text
User named single leaf + clear scope → skip mandatory router ladder.
Leaf-direct: Using `implementation-plan-verify` on the existing plan.
Do not force plan → execute → execute-verify.
```

Optional thin wire (if controller insists):

```text
- [ ] implementation-plan — N/A
- [x] implementation-plan-verify → Meta ready
- [ ] implementation-execute / -subagents — N/A
- [ ] implementation-execute-verify — N/A
```

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `guide-implementation` | **pass** | This run (header + chat) |
| C2 | Classifies impl ask | **pass** | full-ladder / execute-verify / leaf-direct skip |
| C3 | Wire plan with explicit skips (N/A) | **pass** | Parts A–C |
| C4 | Structured handoff fields present | **pass** | Parts A–B (goal/prior/facts/open/constraints) |
| C5 | Compose only — no Plan/Execute spines | **pass** | Leaves named only |
| C6 | Selection ≠ solving — no plan tasks / implement | **pass** | No plan file; no code |
| C7 | Single-leaf → router skip or thin verify-only wire | **pass** | Part C |

## Anti-patterns

| # | Must not | Observed? |
|---|----------|-----------|
| A1 | Restate Theme 6–8 pocket law inline | **no** |
| A2 | Become a second `implementation-plan` (WBS here) | **no** |
| A3 | Force full ladder when verify-only / execute-only | **no** (Part B) |
| A4 | Treat draft design as accepted law | **no** — premise marked hypothetical/accepted for smoke only |

## Pass bar check

| Check | Result |
|-------|--------|
| Announces Using `guide-implementation` | **yes** |
| Different wires for A / B / C | **yes** |
| No Plan/Execute spines in router output | **yes** |
| No WBS in router output | **yes** |
| B not full ladder | **yes** |
| C allows leaf-direct or verify-only | **yes** |
| Theme 20 rename surface id used | **yes** (`guide-implementation`) |

## Verdict

**PASS** — Theme 20 rename smoke; no NEEDS REVISION.
