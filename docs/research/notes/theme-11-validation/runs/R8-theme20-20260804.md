---
title: "R8 Theme 20 run — guide-debug"
status: draft
theme: theme-11-validation
surface_id: R8
lane: theme20-rename
date: 2026-08-04
skill: guide-debug
verdict: PASS
---

# R8 Theme 20 — `guide-debug` (2026-08-04)

**Using `guide-debug`** announced once at session start.  
Classify/wire only — no product debug, no PR/merge. No commit. No edits to skills/ or rules/.

Authority read: `skills/guide-debug/SKILL.md`; Theme 17 accepted report; claim card `r8-debug-router.md` (surface `guide-debug` post Theme 20 rename). Standards resolve gate: no accepted host catalog → no-op.

## Parts A–D (classify / wire)

### Part A — prove-only

**Ask:** minimal failing repro dossier for a flaky test — do not fix yet. Classify and wire.

| Field | Value |
|-------|-------|
| Classify | `prove-only` |
| Seam | n/a |
| Named leaf already? | no |
| Wire | single entry leaf **`debug-reproduce`** |
| Invoke (plan) | **Using `debug-reproduce`** — not executed (smoke: classify/wire only) |

**Handoff (structured):**

```text
Goal: Capture a minimal failing repro dossier for a flaky test; no product fix.
Prior actions / results: none in this smoke; prove-only requested.
Key facts + where from: flaky test (user); no stack/command yet.
Open question for next leaf: What is the smallest reliable failing command/case?
Constraints: no product fix until repro / NOT-YET-REPRODUCED; iron law holds.
```

No Theme 9 spine pasted. No two-step wire.

### Part B — investigate-fix (T-VF, repro known)

**Ask:** Execute verify-fail after N=2; repro command already known. Route for investigate/fix.

| Field | Value |
|-------|-------|
| Classify | `investigate-fix` |
| Seam | **T-VF** (Execute verify-fail; N=2 exhausted) |
| Named leaf already? | no |
| Wire | single entry leaf **`debug-systematic`** |
| Invoke (plan) | **Using `debug-systematic`** — not executed (smoke: classify/wire only) |
| Budget | Do **not** burn more Execute `verify-retry N=2`; Debug uses `debug-fix-cycles` |

**Handoff (structured):**

```text
Goal: Investigate/fix with evidence after Execute verify-fail.
Prior actions / results: Execute verify-retry N=2 exhausted; repro command known.
Key facts + where from: T-VF seam; repro in hand (user).
Open question for next leaf: Root cause under same repro; fix with evidence.
Constraints: no guess-fix; do not reopen Execute N=2; iron law satisfied via known repro.
```

Default one entry leaf — not `debug-reproduce` → `debug-systematic` (repro already known; not T-NYR / not explicit prove-then-fix).

### Part C — negative (PR/merge ceremony)

**Ask:** debug this and open/merge a PR.

**Response (executed):** **Refuse ceremony.**

- Out of scope / handoffs: PR / CI / merge → **Host / human — Phase 2**.
- Classifier hint: “Open a PR” / merge → refuse ceremony; Debug only if a bug remains.
- No PR opened, no merge attempted, no Bugbot/CI loop started.
- If a real bug remained, would classify separately for Debug leaves — not done here (ask bundled ceremony).

### Part D — intelligent skip

**Ask:** just run `debug-reproduce` — I already chose it.

| Field | Value |
|-------|-------|
| Classify | `skip-to-named-leaf` |
| Seam | n/a |
| Named leaf already? | **yes** — `debug-reproduce` |
| Wire | skip router; invoke leaf directly |
| Invoke (plan) | **Using `debug-reproduce`** — not executed (smoke: classify/wire only) |

Documented skip per skill: user already named leaf → invoke that leaf directly.

## Claim scores

| # | Claim | Score | Evidence (short) |
|---|-------|-------|------------------|
| C1 | Announces Using `guide-debug` | **PASS** | Once at session start; skill: “Announce once: **Using `guide-debug`**.” |
| C2 | Classifies prove-only vs investigate-fix | **PASS** | A → `prove-only`; B → `investigate-fix` (T-VF). |
| C3 | Default one entry leaf; optional prove-then-fix wire | **PASS** | A → reproduce only; B → systematic only; no default two-step. |
| C4 | Invokes leaf via Using …; does not paste Theme 9 spine | **PASS** | Wire plans name **Using `debug-reproduce`** / **Using `debug-systematic`**; no Theme 9 method pasted. |
| C5 | Intelligent skip when leaf already named | **PASS** | Part D → `skip-to-named-leaf` → `debug-reproduce` direct. |
| C6 | Refuses PR/merge ceremony | **PASS** | Part C refuse; host/human Phase 2. |

## Anti-patterns

| # | Must not | Observed? |
|---|----------|-----------|
| A1 | Default two-step wire always | **No** — A and B each one entry leaf |
| A2 | Burn Execute verify-retry under Debug | **No** — Part B notes N=2 exhausted; Debug budget only |
| A3 | Guess-fix / skip iron law | **No** — prove-only blocks fix; B has repro in hand |

## Verdict

**PASS**

All claims C1–C6 scored PASS; anti-patterns A1–A3 not observed; Part C refused ceremony as required. Post Theme 20 rename, surface id remains R8; skill name is `guide-debug`.
