---
title: "Theme 11 run log — D2 technical-design"
status: draft
theme: theme-11-validation
surface_id: D2
created: 2026-07-30
---

# D2 — technical-design (pocket smoke)

| Field | Value |
|-------|-------|
| Surface | `technical-design` |
| Date | 2026-07-30 |
| Smoke prompt | Design minimal fix for smoke-app `add()` off-by-one; no code; point to plan next |
| Verdict | **PASS** |

## Smoke design output (this run)

**Using `technical-design`.**

### Observed defect (E0)

- `docs/research/fixtures/smoke-app/app.py`: `add` returns `a + b + 1` with inline `# BUG: off-by-one; correct is a + b`.
- `docs/research/fixtures/smoke-app/test_app.py`: expects `add(2, 3) == 5`.

### Architecture / stack-shaped concerns (minimal ask)

| Concern | Decision for this ask |
|---------|------------------------|
| Deploy / stack shape | Unchanged. Single-module Python fixture; no services, frameworks, or hosting choice. |
| Module / dependency boundary | Keep `add` as a pure function in `app.py`; tests remain the outer consumer (`test_app` → `app`). No new packages or cycles. |
| Interface | Public signature `add(a: int, b: int) -> int` and docstring (“sum of a and b”) stay; only implementation body changes. |
| Patterns / Clean constraints | No GoF pattern needed. Constraint: implementation must match documented contract (docstring + test). |
| ADR trigger? | **No.** One obvious correction; does not affect structure, NFRs, dependencies, or interfaces. Skip `draft-adr`. |

### Minimal fix (design only — not applied)

Change the return expression in `app.py` from `a + b + 1` to `a + b`. No other files, APIs, or layout changes.

### Handoff (next)

→ **`implementation-plan`** (then `implementation-plan-verify` → execute) for the one-line body change + verify via `test_app.py`.  
Do **not** draft an ADR for this defect.

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `technical-design` | pass | In-run announce: “Using `technical-design`.” |
| C2 | Covers architecture/stack-shaped concerns for the ask | pass | Design table covers deploy/stack (unchanged), module/dependency boundary, interface stability, patterns/constraints; scoped to the off-by-one ask. |
| C3 | Hands off to draft-adr / plan when appropriate | pass | Explicitly skips ADR (no structural choice); points next to `implementation-plan` → verify → execute. |

## Anti-patterns

| # | Must not | Observed? | Notes |
|---|----------|-----------|-------|
| A1 | Implement code instead of design | no | No edits to `smoke-app/` or `skills/`/`rules/`; design-only + plan handoff. |

## Evidence quotes (from this run)

1. Announce: `Using \`technical-design\`.`
2. Stack/boundary: `Single-module Python fixture; no services, frameworks, or hosting choice.` / `tests remain the outer consumer (`test_app` → `app`).`
3. ADR skip + plan handoff: `Skip \`draft-adr\`.` / `→ **\`implementation-plan\`** (then \`implementation-plan-verify\` → execute)`
4. Anti-pattern A1: `No edits to \`smoke-app/\``; design states`correct is a + b` without applying the change.

## Verdict rationale

All three claims pass; A1 not observed. Skill stayed in design mode for a trivial defect, covered proportional architecture concerns, declined ADR, and handed off to plan.
