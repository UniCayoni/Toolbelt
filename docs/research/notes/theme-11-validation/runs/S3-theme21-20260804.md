---
title: "S3 — Theme 21 standards fan-out (T19J smoke)"
status: draft
theme: theme-11-validation
surface_id: S3
created: 2026-08-04
lane: in-session
skills:
  - guide-implementation
  - guide-design
  - guide-debug
  - guide-standards
verdict: PASS
aligned_with:
  - docs/research/notes/theme-11-validation/claim-cards/s3-standards-fanout.md
  - docs/research/reports/theme-21-standards-fanout.md
  - docs/research/reports/theme-19-standards-apply.md
---

# S3 — standards fan-out (pocket guides)

Authority: Theme 21 + Theme 19.  
Classify / wire / resolve only — no product implementation.  
Did not edit `skills/` or `rules/`; did not create `docs/standards/`; did not commit.

## Parts

### A — no-op (host catalog absent)

**Using `guide-implementation`**.

Ask: design accepted (hypothetical); wire Implementation for a tiny Toolbelt docs tweak.

| Field | Value |
|-------|-------|
| Classify | `trivial-skip` (one-file docs tweak; careful edit without durable plan) |
| Preconditions | design accepted (hypothetical — documented for smoke) |
| Host catalog | `docs/standards/index.md` — **absent** (E0: zero files under `docs/standards/`) |
| Standards resolve | **no-op** (document) — no invented Toolbelt-universal standards |

**Wire (sketch, not invoked):** skip durable plan ladder; document trivial-skip. No `guide-standards` announce (resolve gate no-ops before resolve).

**Structured handoff (excerpt):**

```text
goal: tiny Toolbelt docs tweak
standards_catalog: (absent)
standards_modules: (none — resolve no-op)
constraints: draft ≠ law; no invented host coding law
```

### B — pointers (fixture catalog + Implementation)

**Using `guide-implementation`**.

Catalog override: `docs/research/notes/theme-11-validation/runs/artifacts/t19-catalog/index.md` (status **accepted**, smoke fixture only — not Toolbelt host law).  
Ask: implement a Cursor skill helper under `skills/`.

| Field | Value |
|-------|-------|
| Classify | `full-ladder` lean (new skill surface; non-trivial) → plan → plan-verify → execute → execute-verify |
| Paths / skills | `skills/**`; Implementation pocket |
| Standards resolve | accepted catalog → **Using `guide-standards`** |

**Using `guide-standards`** (resolve only — pointers, not rule dumps).

Classifier:

```text
Action: implement-skill-helper
Wording: implement a Cursor skill helper under skills/
Caller pocket: guide-implementation
Skill id: guide-implementation / impl
Paths touched / likely: skills/**
Perceived intent: select matching technical modules — not dump corpus
```

**Structured handoff (pointers only):**

```text
standards_catalog: docs/research/notes/theme-11-validation/runs/artifacts/t19-catalog/index.md
standards_modules:
  - id: skill-surfaces
    path: docs/research/notes/theme-11-validation/runs/artifacts/t19-catalog/modules/skill-surfaces.md
    reason: path skills/** + pocket/skills guide-implementation|impl (catalog row)
```

Module rule table **not** pasted in router output. Progressive load deferred to leaf/Task consumers.

### C — pocket lean (Design + same fixture)

**Using `guide-design`**.

Catalog: same fixture as Part B. Fixture module `skill-surfaces` tags: `author-cursor-surfaces`, `guide-standards`, `guide-implementation`, `impl` — **no** `design` / `guide-design` tag.

| Field | Value |
|-------|-------|
| Standards resolve | catalog present → **Using `guide-standards`** for Design pocket match |
| Module match | **empty** — Impl technical row does not match Design pocket |
| Result | **no auto-load** of Impl technical modules; pocket lean (empty match / effective no-op for modules) |

**Handoff:**

```text
standards_catalog: docs/research/notes/theme-11-validation/runs/artifacts/t19-catalog/index.md
standards_modules: []  # empty match — Design does not auto-load Impl technical
```

Design spine continues flexible (explore/clarify/options) without Impl naming/layout law. No module body dump.

### D — already-pinned (Debug skip re-resolve)

**Using `guide-debug`**.

Assume `standards_modules` already pinned this turn from Part B (`skill-surfaces` pointer).

| Field | Value |
|-------|-------|
| Classify | `prove-only` |
| Seam | none (smoke); prove-only path |
| Standards resolve | **skip** (document) — already pinned; do not re-invoke `guide-standards` |
| Wire | `debug-reproduce` only; skip `debug-systematic` |

**Wire plan:**

```text
1. debug-reproduce  (prove-only)
2. debug-systematic — N/A (not investigate-fix)
standards_modules: (carry Part B pin — no re-resolve)
```

## Claim scores

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | `guide-implementation` documents resolve done \| no-op \| already-pinned | **pass** | Skill step 3; Parts A (no-op), B (resolve), D documents already-pinned pattern on Debug |
| C2 | Absent catalog → **no-op** (no invented law) | **pass** | Part A — no `docs/standards/` |
| C3 | Fixture catalog → pointers; bodies not dumped | **pass** | Part B — `skill-surfaces` pointer only |
| C4 | Research/design lean: no auto Impl technical without tag match | **pass** | Part C — empty `standards_modules` for Design |
| C5 | Already-pinned → skip re-resolve | **pass** | Part D |
| C6 | Announces Using `guide-standards` when resolving | **pass** | Part B (and Part C classify announce); Part A correctly omitted |

## Anti-patterns

| # | Must not | Observed? |
|---|----------|-----------|
| A1 | Full standards dump | **no** — pointers only; module body not in router output |
| A2 | Toolbelt-universal law when absent | **no** — Part A no-op |
| A3 | Four full reloads on happy-path without skip | **no** — Part D skip re-resolve |

## Verdict

**PASS** — C1–C6 pass; A1–A3 not observed.
