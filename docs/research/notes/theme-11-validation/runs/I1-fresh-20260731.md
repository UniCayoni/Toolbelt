---
title: "Theme 11 run log — I1-fresh implementation-router (Theme 14 smoke)"
status: draft
theme: theme-11-validation
surface_id: I1
created: 2026-07-31
aligned_with:
  - docs/research/notes/theme-11-validation/claim-cards/i1-implementation-router.md
  - docs/research/reports/theme-14-pocket-routers.md
---

# I1-fresh — implementation-router (Theme 14 smoke)

**Using `implementation-router`**.

| Field | Value |
|-------|-------|
| Surface | `implementation-router` |
| Date | 2026-07-31 |
| Lane | in-session pocket/controller |
| Scope | Routing / checklist / handoff only — no plan file, no code, no `skills/` edits |
| Verdict | **PASS** |

## Part A — full feature wire

Ask: design for smoke-app add() off-by-one fix human-accepted (given); wire Implementation pocket only; do not plan or code.

| Field | Value |
|-------|-------|
| Impl ask | **full-ladder** |
| Design/ADR | Human-accepted design treated as given for smoke — no new product locks invented |
| Trivial skip | no |

Structured handoff:

```text
Goal: Wire Implementation pocket for smoke-app add() off-by-one fix after accepted design.
Prior actions / results: Design gate treated as human-accepted (smoke given).
Key facts + where from: Fixture = add() off-by-one (Theme 11 smoke-app lineage / claim card I1 Part A).
Open question for next leaf: Durable plan path + Meta ready before execute; Done-when/Verify for the fix.
Constraints: Routing / checklist / handoff only — do not write plan body; do not implement.
```

Wire plan:

```text
- [x] implementation-plan
- [x] implementation-plan-verify → Meta ready
- [x] implementation-execute (single-worker lean for small fix)
- [ ] implementation-execute-subagents — N/A (not multi-task / fresh-agents ask)
- [x] implementation-execute-verify (non-trivial / EOP)
- [ ] On verify-fail / unclear Critical → debug-reproduce / debug-systematic — N/A until a leaf exits fail
```

Compose note: leaves named only; **no** Plan/Execute spines pasted. Selection ≠ solving: **no** WBS/tasks authored here. Leaves not invoked (smoke stop after wire).

## Part B — verify-only

Ask: Meta ready plan exists; greens done; need EOP `implementation-execute-verify` only.

| Field | Value |
|-------|-------|
| Impl ask | **execute-verify** |

Structured handoff:

```text
Goal: Run EOP implementation-execute-verify only on Meta-ready plan with greens done.
Prior actions / results: Plan exists; Meta ready; task greens done (smoke given).
Key facts + where from: Claim card I1 Part B / user ask.
Open question for next leaf: Verify pass/fail and any Critical gaps for human or happy-path next stage.
Constraints: Wire with skips; do not implement; do not re-run plan/execute.
```

Wire plan:

```text
- [ ] implementation-plan — N/A (plan exists)
- [ ] implementation-plan-verify — N/A (Meta ready given)
- [ ] implementation-execute — N/A (greens done)
- [ ] implementation-execute-subagents — N/A
- [x] implementation-execute-verify
- [ ] Debug handoff — only if verify-fail later
```

Not a full ladder. Leaf not invoked (smoke stop after wire).

## Part C — single leaf / skip

Ask: only run `implementation-plan-verify` on the existing plan.

| Field | Value |
|-------|-------|
| Impl ask | **plan+verify** lean → skill documents **skip router** when user named a single clear leaf |
| Exit | Leaf-direct: **Using `implementation-plan-verify`** — router not mandatory |

Documented skip:

```text
User named single leaf + clear scope → skip mandatory router ladder.
Leaf-direct: Using `implementation-plan-verify` on the existing plan.
Do not force plan → execute.
```

Optional thin wire (if controller insists):

```text
- [ ] implementation-plan — N/A
- [x] implementation-plan-verify → Meta ready
- [ ] implementation-execute / -subagents — N/A
- [ ] implementation-execute-verify — N/A
```

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `implementation-router` | **pass** | This run (header + chat) |
| C2 | Classifies impl ask | **pass** | full-ladder / execute-verify / leaf-direct skip |
| C3 | Wire plan with explicit skips (N/A) | **pass** | Parts A–C |
| C4 | Structured handoff fields present | **pass** | Parts A–B (goal/prior/facts/open/constraints) |
| C5 | Compose only — no Plan/Execute spines | **pass** | Leaves named only |
| C6 | Selection ≠ solving — no plan tasks / implement | **pass** | No plan file; no code |
| C7 | Single-leaf → router skip or thin verify-only wire | **pass** | Part C |

## Anti-patterns

| # | Must not | Observed? |
|---|----------|-----------|
| A1 | Restate Theme 6–8 pocket law inline | **no** |
| A2 | Become a second `implementation-plan` (WBS here) | **no** |
| A3 | Force full ladder when verify-only / execute-only | **no** (Part B) |
| A4 | Treat draft design as accepted law | **no** — premise marked given/accepted for smoke only |

## Pass bar check

| Check | Result |
|-------|--------|
| Announces Using `implementation-router` | **yes** |
| Different wires for A / B / C | **yes** |
| No Plan/Execute spines in router output | **yes** |
| No WBS in router output | **yes** |
| B not full ladder | **yes** |
| C allows leaf-direct or verify-only | **yes** |

## Verdict

**PASS**
