---
title: "Theme 11 run log — G1 systematic-debug"
status: draft
theme: theme-11-validation
surface_id: G1
created: 2026-07-30
---

# G1 — systematic-debug (pocket smoke)

| Field | Value |
|-------|-------|
| Surface | `systematic-debug` |
| Date | 2026-07-30 |
| Work dir | `docs/research/notes/theme-11-validation/runs/artifacts/work-g1/` |
| Verdict | **PASS** |

## Smoke prompt (executed)

> Copy smoke-app to runs/artifacts/work-g1/. Using `systematic-debug`, investigate failing test_app.py (add off-by-one). Reproduce, hypothesize, fix minimally, re-verify. Do not invent Cursor private APIs.

## Smoke result (one line)

Copied fixture → failing repro (`AssertionError`, `add(2,3)=6`) → H1 CONFIRMED → minimal fix in work-g1 only → same repro green (`ok`); fixture source left buggy.

## Spine followed

1. **Intake** — expected `add(2,3)==5`; surface = unit test.
2. **Reproduce** — ran `test_app.py` in work-g1 → `AssertionError` before any patch.
3. **Evidence path** — Terminal only (local clear fail); no Cursor private debug-server APIs.
4. **Hypothesize / falsify**

| ID | Hypothesis | Status | Evidence |
|----|------------|--------|----------|
| H1 | `add` returns `a+b+1` (off-by-one) so `add(2,3)==6` not `5` | CONFIRMED | Direct call printed `add(2,3)= 6`; source line `return a + b + 1` |

5. **Root cause** — Extra `+ 1` in `work-g1/app.py` `add()`.
6. **Minimal fix** — `return a + b` in work copy only.
7. **Verify same repro** — re-ran `python .../work-g1/test_app.py` → `ok`; `add(2,3)=5`.
8. **Stop** — fixed in **debug-fix-cycles = 1** (of skill budget 3). This budget is **not** Execute `verify-retry N=2`.

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `systematic-debug` | pass | Exact announce in-run: “Using `systematic-debug`.” |
| C2 | Reproduce before fix (or NOT-YET) | pass | Pre-fix: `AssertionError` on `assert add(2, 3) == 5`; measured `add(2,3)= 6`. |
| C3 | Hypothesis falsify with evidence | pass | H1 table above: CONFIRMED with printed `6` + source `+ 1`. |
| C4 | Separates debug-fix-cycles from Execute N=2 | pass | Spine §8: used debug-fix-cycles=1; explicitly not Execute verify-retry N=2. |
| C5 | Verify same repro after fix | pass | Same `python .../test_app.py` → `ok`; post-fix `add(2,3)= 5`. |

## Anti-patterns

| # | Must not | Observed? | Notes |
|---|----------|-----------|-------|
| A1 | Fix from code-read alone without repro | no | Terminal repro + measured `6` before patch. |
| A2 | Invent Cursor private debug-server API | no | Terminal evidence path only; no private APIs. |

## Evidence quotes (from this run)

1. Announce: `Using \`systematic-debug\`.`
2. Pre-fix fail: `AssertionError` on `assert add(2, 3) == 5`.
3. Measured: `add(2,3)= 6` / `expected 5`.
4. Post-fix same repro: `ok` and `post-fix add(2,3)= 5`.
5. Fixture source intact: `docs/research/fixtures/smoke-app/app.py` still `return a + b + 1`.
6. Cycle budget note: `debug-fix-cycles = 1` ≠ Execute `verify-retry N=2`.

## Verdict rationale

All five claims pass; A1/A2 not observed. Skill announced, reproduce-before-fix held, H1 falsified with terminal evidence, debug-fix budget kept distinct from Execute N=2, and the same repro verified green after a minimal work-dir-only fix.
