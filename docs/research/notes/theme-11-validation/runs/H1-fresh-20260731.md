---
title: "Theme 11 run log — H1 happy-path (Theme 14 fresh re-smoke)"
status: draft
theme: theme-11-validation
surface_id: H1
created: 2026-07-31
aligned_with:
  - docs/research/notes/theme-11-validation/claim-cards/h1-implementation-happy-path.md
  - docs/research/reports/theme-14-pocket-routers.md
---

# H1 — implementation-happy-path (fresh re-smoke)

**Using `implementation-happy-path`**.

| Field | Value |
|-------|-------|
| Surface | `implementation-happy-path` |
| Date | 2026-07-31 |
| Lane | fresh_chat |
| Claims focus | C7–C8 (Theme 14) + C1–C6 regression |
| Verdict | **PASS** |

Classifier + checklist stages only. No gather / design / implement. No edits under `skills/`.

---

## Part A — Bug: smoke-app `add()` off-by-one

Ask: fix smoke-app `add()` off-by-one end-to-end.

```text
Ask type: bug
Entry skill / skip notes:
  Entry → debug-reproduce and/or debug-systematic
  research-scope → Skip (clear bug; not expand-first)
  design-process → Skip documented (pure bug-fix + clear repro)
  implementation-router → N/A until debug returns to implement
  Not forced through implementation-router first
```

```text
Happy-path Progress:
- [x] 0 Classified ask → bug
- [ ] 1 Research as needed — N/A
- [ ] 2 design-process → skip documented (bug + clear repro)
- [ ] 3 research-draft-adr — N/A
- [ ] 4 implementation-router — N/A unless debug returns to implement
- [x] 5 Debug branch → debug-reproduce / debug-systematic (Debug router deferred)
- [ ] 6 Stop / human handoff
```

Workers = **one pocket leaf** (e.g. `debug-reproduce` or `debug-systematic`), not happy-path.

**Expect met:** bug → debug leaves; not forced through `implementation-router` first.

---

## Part B — Feature: non-trivial, needs design then implementation

Ask: non-trivial feature that still needs design then implementation (checklist stages only).

```text
Ask type: feature
Entry skill / skip notes:
  Happy path below
  Research → as needed (N/A if familiar / no docs pin / no deep campaign)
  Design → design-process → domain design → human accept (required)
  ADR → optional if locks need recording
  Implementation → implementation-router (pocket router; not leaf dump as SoT)
```

```text
Happy-path Progress:
- [x] 0 Classified ask → feature
- [ ] 1 Research as needed — research-scope (optional) → research leaves — or N/A if familiar
- [x] 2 design-process → domain design → human accept
- [ ] 3 research-draft-adr if locks — or N/A
- [x] 4 implementation-router (wires plan → plan-verify → execute|-subagents → execute-verify)
- [ ] 5 Debug branch if needed — N/A until verify-fail
- [ ] 6 Stop / human handoff
```

Compose check: Implementation stage is **`implementation-router`**. Does **not** re-list plan → plan-verify → execute → execute-verify leaves as duplicate SoT here. No Execute/Debug spines pasted.

Workers = **one pocket leaf** after controller routes (not full happy-path).

**Expect met:** Implementation stage = `implementation-router` (C7).

---

## Part C — Research: multi-surface theme campaign

Ask: multi-surface theme campaign on “how agents should scope research tracks” — checklist only; do not gather.

```text
Ask type: research-only
Entry skill / skip notes:
  Tracks unclear / expand-first → optional research-scope
  Then research leaves via that skill’s handoffs
    (research-codebase-recon / research-docs / research-protocol)
  Continue to design only after accept
  Full feature ladder not started this smoke
```

```text
Happy-path Progress:
- [x] 0 Classified ask → research / theme campaign
- [x] 1 Research as needed — research-scope (optional; tracks unclear) → research leaves
- [ ] 2 design-process — only after research accept (not this smoke)
- [ ] 3 research-draft-adr — N/A until locks after accept
- [ ] 4 implementation-router — N/A
- [ ] 5 Debug branch — N/A
- [ ] 6 Stop / human handoff (after research accept or note delivery)
```

Workers = **one pocket leaf** (e.g. one research leaf), not happy-path.

**Expect met:** optional `research-scope` → research leaves; design only after accept (C6).

---

## Part D — Negative: ADR-only for logging

Ask: only draft an ADR for logging.

```text
Ask type: authoring (ADR leaf; not feature ladder)
Entry skill / skip notes:
  Entry → research-draft-adr
  Full Research → Design → Implementation ladder → not mandatory
  design-process / implementation-router → N/A for this ask
```

```text
Happy-path Progress:
- [x] 0 Classified ask → authoring / ADR-only
- [ ] 1 Research — N/A (not a theme campaign)
- [ ] 2 design-process — N/A (not a feature ask)
- [x] 3 research-draft-adr — entry for this ask
- [ ] 4 implementation-router — N/A
- [ ] 5 Debug branch — N/A
- [ ] 6 Stop / human handoff
```

**Expect met:** `research-draft-adr` / authoring — not mandatory full feature ladder.

---

## Part E — Implementation-only

Ask: design already accepted; wire Implementation pocket only.

```text
Ask type: implementation-only
Entry skill / skip notes:
  Prefer implementation-router
  Skip full Research → Design ladder (design already accepted)
  research-scope / design-process → N/A this path
```

```text
Happy-path Progress:
- [x] 0 Classified ask → implementation-only
- [ ] 1 Research — N/A (skip full ladder)
- [ ] 2 design-process — N/A (already accepted; documented skip of re-ladder)
- [ ] 3 research-draft-adr — N/A unless new locks appear
- [x] 4 implementation-router (plan → plan-verify → execute|-subagents → execute-verify)
- [ ] 5 Debug branch if needed
- [ ] 6 Stop / human handoff
```

Workers = **one pocket leaf** under the router wire (not happy-path as worker skill).

**Expect met:** prefer `implementation-router` over full Research→Design ladder (C8).

---

## Claims

| # | Claim | Score | Evidence |
|---|-------|-------|----------|
| C1 | Announces Using `implementation-happy-path` | **pass** | This run |
| C2 | Classifies ask type | **pass** | bug / feature / research-only / authoring / implementation-only |
| C3 | Compose via existing skills (no pasted pocket law) | **pass** | Routers/entries only; no Execute/Debug spines |
| C4 | Research as-needed preface | **pass** | N/A on A/D/E; as-needed on B; scope on C |
| C5 | Workers = one pocket leaf | **pass** | Stated each part |
| C6 | Optional `research-scope` when tracks unclear | **pass** | Skip A; used C; not forced elsewhere |
| C7 | Feature path uses `implementation-router` | **pass** | Part B stage 4 |
| C8 | Implementation-only prefers `implementation-router` | **pass** | Part E |

## Anti-patterns

| # | Must not | Observed? |
|---|----------|-----------|
| A1 | Re-teach full Execute/Debug spines inline | **no** |
| A2 | Skip human design gate without documenting skip | **no** — documented on A/E |
| A3 | Force `research-scope` on every research/bug path | **no** |
| A4 | Bypass Theme 14 by pasting old Implementation leaf ladder as SoT | **no** — stage 4 is router |

## Distinct routes (pass bar)

| Part | Ask type | Entry |
|------|----------|-------|
| A | bug | `debug-reproduce` / `debug-systematic` |
| B | feature | ladder → `implementation-router` at stage 4 |
| C | research-only | optional `research-scope` → research leaves |
| D | authoring | `research-draft-adr` |
| E | implementation-only | `implementation-router` (no full ladder) |

## Verdict

**PASS** — C7/C8 held; distinct routes; no Execute/Debug spines; workers = one pocket leaf.
