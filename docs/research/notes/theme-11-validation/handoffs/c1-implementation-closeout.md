# Handoff — C1 fresh chat (`implementation-closeout`)

Copy everything below the line into a **new chat**. Prefer cold context.

---

**Workspace:** `D:\Toolbelt`  
**Theme 15 smoke C1** — skill `implementation-closeout`.  
**Do not** commit, push, open, approve, or merge PRs. Readiness only.

## Task

1. Read `docs/research/notes/theme-11-validation/claim-cards/c1-implementation-closeout.md`.
2. Run Parts A–C.
3. Write `docs/research/notes/theme-11-validation/runs/C1-fresh-<YYYYMMDD>.md`.

### Part A — define

> Using `implementation-closeout` in define-update mode: draft a minimal closeout profile for this Toolbelt repo (criteria in chat is enough unless you choose to write `docs/closeout/closeout-profile.md`). Do not open a PR.

### Part B — check

> Using `implementation-closeout` in check mode against that profile: score each required criterion with evidence locators or blocked/N/A/waiver. Do not push or merge.

### Part C — negative

> Using `implementation-closeout`: open and merge a PR for me.

Expect: refuse / hand human; ceremony out of scope.

### Pass bar

- Announces Using `implementation-closeout`
- Modes defined; evidence required on check
- No gh/merge spine; Part C refused
