# Theme 11 Phase B handoffs

Paste each prompt into a **new Cursor chat** (Toolbelt plugin loaded).

| File | Chat purpose |
|------|----------------|
| `controller-pocket-smokes.md` | Parent controller: launch subagents for R/D/P/E/G cards |
| `u1-draft-is-not-sot.md` | Fresh discovery — always-on draft≠SoT |
| `u2-research-protocol-grades.md` | Fresh discovery — grades / cite-or-omit |
| `h1-implementation-happy-path.md` | Fresh discovery — happy-path (+ optional implicit/negative) |

## After runs — export evidence

Export each chat transcript into:

`docs/research/notes/theme-11-validation/runs/exports/<YYYY-MM-DD>/`

Also keep structured run logs under `runs/` (`runs/<ID>-YYYYMMDD.md`).

Coordinator scores claim cards from `runs/` (structured logs + exports).
